{
    "tasks": [
        {
            "label": "cmake",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "command": "cmake --build ${workspaceFolder}/build/",
        },
        {
            "label": "cmake tests",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/tests/build"
            },
            "command": "cmake --build ${workspaceFolder}/tests/build/",
        },

    ],

    "version": "2.0.0"
}